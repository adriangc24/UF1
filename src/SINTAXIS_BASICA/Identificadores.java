package SINTAXIS_BASICA;

public class Identificadores {

	public static void main(String[] args) {
		int _a; // Si esta declarada porque empieza con un _
		int ac;	// Si esta declarada porque empieza por una letra
		double $real;	// Si esta declarada porque empieza con un simbolo del $
		float final;	// No se puede declarar porque es una sintaxis reservada
		float Diners; // Si se puede declarar porque float es un numero decimal
		char #x;	// No se puede declarar porque char es una variable de tipo caracter
		char y#;	// No se puede declarar porque char es una variable de tipo caracter
		char "edad"; // No se puede declarar porque char es una variable de tipo caracter no String
	}

}
